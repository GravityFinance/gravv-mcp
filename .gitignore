node_modules/
dist/
*.log

# Never commit a key. The server reads GRAVV_API_KEY from the environment.
.env
.env.*
!.env.example

# An .npmrc here would hold a registry auth token. Publishing uses OIDC in CI, so
# this should only ever exist transiently during a local publish.
.npmrc

# npm pack output
*.tgz
